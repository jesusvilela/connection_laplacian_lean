# Lean / Lake
.lake/
build/
*.olean
*.ilean
*.trace

# LaTeX intermediate output (paper.pdf is committed; everything else is disposable)
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.nav
paper/*.snm
paper/*.vrb

# Local LaTeX engine copies — users install their own
tools/tectonic.exe
tools/tectonic.zip
tools/*.exe

# Python fuzzer caches / venvs
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Secrets — belt and suspenders, none present at scaffold time
.env
.env.*
*.key
*.pem
secrets/

# Round 7 research kept local-only pending its own round discipline
findings/round7/

# Parallel research tracks kept local-only until they clear their own round discipline
ConnectionLaplacian/U1/
ConnectionLaplacian/Z3/
ConnectionLaplacian/Zk/

# Temporary fuzzer output directories
tmp_*/
